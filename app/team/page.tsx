import TeamDirectoryClient from '@/components/member/TeamDirectoryClient'

export default function TeamPage() {
  return (
    <main className="min-h-screen bg-[var(--surface-bg)]">
      <TeamDirectoryClient />
    </main>
  )
}
